rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /recipes/{allPaths=**} {
      // Allow anyone to read and write recipe images
      allow read, write: if true;
    }
  }
}
